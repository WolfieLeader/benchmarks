apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8181
    jsonData:
      version: SQL
      dbName: benchmarks
      httpMode: POST
      insecureGrpc: true
    isDefault: true
