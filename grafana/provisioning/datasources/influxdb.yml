apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: benchmarks
      defaultBucket: benchmarks
    secureJsonData:
      token: benchmark-token
    isDefault: true
